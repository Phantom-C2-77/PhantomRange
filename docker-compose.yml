services:
  phantomshop:
    build: .
    container_name: phantomshop
    ports:
      - "9000:9000"
    volumes:
      - phantomshop-data:/app/data
    restart: unless-stopped

volumes:
  phantomshop-data:
